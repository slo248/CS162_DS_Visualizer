if (head==nullptr) return;
if (head->next==nullptr){
  delete head;
  head=nullptr;
} 
Node* pre = head, *tail = head->next;
while (tail->next != null)
  pre = pre->next;
pre->next = nullptr;
delete tail;